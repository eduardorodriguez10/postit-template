<h4>This are the current categories</h4>

<% @categories.each do |category| %>
	<div class='row'>
		<div class='span8 well'>
			<h4><%= link_to category.name, edit_category_path(category) %></h4>
		</div>
	</div>
<% end %>